// e2eeCopy.ts — 설정(종단간 암호화)·기기 승인 화면의 **문구 계약 정본 미러**.
//
// 정본 = `docs/구현설계-2026-07-25/14-설정-카피-감사.md` §4. 앱과 PC 가 **글자까지 같은 문구**를 써야
//  한다(사용자가 폰과 PC 를 나란히 놓고 대조한다 — 한 글자 차이가 곧 버그로 읽힌다). 그래서 화면
//  컴포넌트에 문자열을 흩뿌리지 않고 여기 한 곳에 모은다: 회귀 테스트(`__tests__/e2eeCopy.test.ts`)가
//  이 표를 고정하고, 삭제한 16개 문구가 소스에 다시 나타나지 않는지도 함께 단정한다.
//
// ★ 축약해도 절대 지워지지 않는 것(§5 — 지우면 보안이 무너진다):
//   · `appr.instr`   두 화면 눈 대조가 서버 MITM 차단의 전부(§2.10). '글자까지' + '다르면 거절' 유지.
//   · `appr.reqno`   4자리(13비트)는 서버가 1.3초에 위조한다 → **대조 대상이 아님**을 라벨에 붙인다.
//   · `appr.noSafety` 대조 기준 없이 승인하는 습관을 막는다(+ 승인 버튼 disabled).
//   · `appr.unverified` 표시값이 서버 지배 상태(verified=false)라는 신호.
//   · host 배지 4종 의미(§2.7 거짓 자물쇠 금지 — '모름' 을 '평문' 으로 단정하지 않는다).
//   · self 배지에 '켜짐' 을 쓰지 않는다(자기 열쇠 보유 ≠ 트래픽 암호화).
//   · `row.revokeArm` 비가역 경고(결정 순간에만).
//  (★ 개정 4 로 `adv.*` — 정책·안전 코드 상시 행·복구·메타 고지 — 는 UI 째로 삭제됐다. §3 개정 4 블록 참조.)
//
// ⚠ 배지 문구(self/host)의 **산출**은 `services/e2ee/e2eeState.ts` 가 리터럴로 갖는다(PC 교차검증이
//  함수 본문만 오려 실행하므로 상수 참조를 넣을 수 없다). 여기 값은 그 산출과 같아야 하고,
//  `e2eeCopy.test.ts` 가 두 곳의 일치를 단정한다.

export const E2EE_COPY = {
  //  ★ 2026-07-27 개정 2(사용자 요구): '종단간 암호화' 카드와 '내 기기' 목록을 **한 섹션**으로 합쳤다.
  //   같은 기기가 두 목록(열쇠를 가진 기기 · 내 기기)에 중복 등장했고, 열쇠 상태는 기기의 속성이므로
  //   기기 행이 단일 진실이다 → 섹션 제목은 `기기`, 기능명(`종단간 암호화`)은 자세히 안 정책 행 제목이
  //   맡는다(그 행이 곧 기능 스위치다). 제목 우측 배지는 그대로 **계정 열쇠 상태**(self 배지)다.
  //  `noHost` = **온라인** host 행이 0개인 상태(PC 를 꺼 뒀거나 아직 연결 전). 그 자리를 비우면 초록
  //   self 배지만 남아 사용자는 '내 데이터가 안전하다' 로 읽는데, 실제 의미는 '이 폰에 열쇠가 있다'
  //   뿐이다(§2.7 정직성 기제가 화면에서 사라진다) → 한 행을 그려 기제를 유지한다.
  //  개정 7: 목록을 `이 기기` / `다른 기기` 로 나눈다(사용자 요구: 목록에 이 기기까지 넣으니 복잡하다).
  //   `noHost`(연결된 PC 없음)는 self 배지와 함께 폐기됐다 — 그 행은 초록 배지를 정직하게 보정하려고
  //   존재했고(§2.7), 배지가 사라졌으므로 남길 이유도 사라졌다. 빈 목록은 `noOther` 한 줄로 말한다.
  //  개정 9: 카드 제목 `기기` 는 삭제됐다 — `이 기기`/`다른 기기` 가 **그룹(카드) 자체**다(사용자 요구:
  //   "이기기, 다른 기기로 그룹을 나눠서 해주고"). 제목을 남기면 계층이 3겹(기기 > 이 기기 > 행)이 된다.
  card: { thisDevice: '이 기기', otherDevices: '다른 기기', noOther: '연결된 기기가 없어요' },

  // 카드 제목 행 우측 배지(self) — e2eeState.stateLabel() 산출과 동일해야 한다.
  selfBadge: {
    ready: '열쇠 있음',
    pending: '승인 대기',
    checking: '확인 중',
    nokey: '열쇠 없음', // PC 전용 산출(keyState=none & checking=false = 영구 평문)
    off: '꺼짐',
    unsupported: '미지원',
    unavailable: '사용 불가',
    error: '오류',
  },

  // PC 별 행 배지(host) — e2eeState.hostLockLabel() 산출과 동일해야 한다.
  hostBadge: {
    encrypted: '암호화됨',
    hostPlain: '평문(열쇠 없음)',
    checking: '확인 중',
    selfPlain: '평문',
  },

  // 행동 행 — 동시 1개만. 우선순위: approve > selfWait > bootstrapping(자동) > needUpdate
  //  ★ 개정 4(2026-07-27 사용자 확정, 카피 감사 §3 개정 4 블록): `adv.*`(자세히 — 정책 세그·안전 코드
  //   상시 행·복구 코드·메타 고지)는 **통삭제**됐다. 정책은 '자동' 고정(env 킬스위치만), 부트스트랩은
  //   상호작용 화면이 자동 수행, 복구 코드는 현 스코프(rpc)에 잠긴 저장 데이터가 없어 지킬 자산이
  //   없다(데몬 RPC 는 존치 — 스냅샷 봉인을 켜는 날 UI 만 되살린다). selfWaitHint(복구 경로 안내)도
  //   복구 UI 와 함께 삭제 — 기기 전손실 = 새 기기에서 자동으로 새 열쇠가 생긴다.
  act: {
    //  (★ 개정 9(2026-07-28 사용자 확정): `approve`(새 기기 N대가 승인을 기다려요 · 알림에서 승인할 수
    //   있어요) 는 **삭제**됐다 — 원문 "이런 멘트는 필요 없을 거 같은데? 만약 저런 승인이 왔다면 다른 기기
    //   목록에 있는 기기 중에 하나에서 보낸 거겠지? 그렇다면 그 기기 미확인 알림처럼 표현해주고 …
    //   클릭 시 아까 처음 이미지처럼 상단에 승인하는 알림이 뜨게 해줘!" → 요약 줄 대신 **그 기기 행**이
    //   말한다(`row.waitingApproval` + 미확인 점 + 탭 → 승인 표면). 사실을 두 군데서 말하지 않는다.)
    //  개정 5: 대기 안내는 "누를 곳"을 기기 종류로 말한다(폰은 PC 를, PC 는 폰을 가리킨다) —
    //   `내 다른 기기` 같은 중립어는 사용자가 무엇을 켜야 할지 못 고른다.
    selfWait: '내 PC에서 승인해 주세요',
    bootstrapping: '암호화를 준비하고 있어요…',
    bootstrapFail: '암호화를 켜지 못했어요 · 잠시 후 다시 시도합니다',
    needUpdate: '앱을 업데이트하면 켜집니다',
  },

  //  기기 행(첫 화면 목록) 문구 — 개정 2 에서 `adv.keys.*` 에서 올라왔다. 구 `adv.keys.title`
  //   ('열쇠를 가진 기기')은 **삭제**다: 같은 기기가 그 목록과 '내 기기' 에 두 번 나왔고, 열쇠 보유는
  //   기기의 속성이므로 행의 지문(🔒)·배지로 흡수했다(목록 하나 = 단일 진실).
  //  `revokeArm` 은 **열쇠를 가진 기기를 지울 때만** 뜬다: 그 삭제는 열쇠 무효화 + 세대 회전을 부르므로
  //   진짜로 되돌릴 수 없다(열쇠 없는 기기는 다시 연결하면 되므로 그 경고를 붙이면 거짓말이 된다).
  //  개정 6: 기기 행은 **연동 상태**를 말한다(암호화 배지 [평문]/[암호화됨] 은 사용자 요구로 삭제 —
  //   "저런 정보는 필요 없잖아"). 승인 절차를 끝내지 않은 기기 = `notLinked` + [연동] 버튼.
  row: {
    mine: '이 기기',
    revokeArm: '다시 눌러 해제 · 되돌릴 수 없음',
    notLinked: '연동 안 됨',
    //  ★ 개정 9: 그 기기가 승인을 기다린다 = 이 행이 **미확인 알림**이다(점 + 탭하면 승인 표면).
    waitingApproval: '승인 대기',
    wasLinked: '이전에 연동된 기기',   // 기기 행이 없는 열쇠(예외) — 지문 숫자 대신 이 말만 적는다
    link: '연동',
    linking: '요청 중…',
    linkSent: '요청 보냄',
  },

  sheet: { title: '기기 승인', empty: '승인할 기기가 없어요' },

  //  ★ 2026-07-28 개정 5(사용자 확정) — 승인 화면을 **구글 로그인 확인**과 같은 구성으로 바꿨다.
  //   사용자 지적: "저렇게 보이면 코드를 입력해야 할 것 같잖아? 다른 기기에서 로그인하면 기존 기기에
  //   본인이 맞나요? 가 뜨고 승인/본인이 아니에요 를 누르는 식 아니야?"
  //   → 평소 = [기기명·시각] + `ask` + [승인][본인이 아니에요]. 60비트 안전 코드는 `reveal`(코드 확인)
  //   안으로 접었다. 대조 채널은 **없애지 않았다**: 코드 대조가 서버 MITM 을 막는 유일한 수단이므로
  //   링크 한 번으로 언제나 꺼낼 수 있어야 한다(시그널 안전 번호와 같은 등급 = 있지만 강요하지 않음).
  appr: {
    head: '새 기기에서 로그인했어요',
    ask: '본인이 맞나요?',
    // [필수-보안] 개정 4: "항상 같은 걸 왜 물어보나"(실제 사용자 질문)의 '왜'를 담았다 — 같음이
    //  정상이고, 다름 = 연결 위조 신호라는 것까지가 지침이다. '승인/거절' 두 행동 지시는 유지.
    //  개정 5: 이 문장은 `reveal` 을 펼쳤을 때만 보인다(대조를 실제로 하려는 사람에게만 필요하다).
    instr: '새 기기 화면에도 같은 코드가 보이면 승인하세요. 정상이라면 항상 같아요 — 다르면 연결이 안전하지 않은 것이니 거절하세요.',
    reqno: (code: string) => `요청 ${code} · 대조용 아님`,
    //  '거절' → '본인이 아니에요': 무엇을 거절하는지가 아니라 **사용자가 판단할 사실**을 적는다.
    deny: '본인이 아니에요',
    approve: '승인',
    reveal: '코드 확인',
    unverified: '요청 번호는 서버 값 · 코드로만 대조하세요',
    noSafety: '안전 코드를 아직 못 만들었어요 · 승인하지 마세요',
  },

  //  ★ `wait.noSafety` 는 `appr.noSafety` 와 **다른 문구여야 한다**: 대기 화면은 새 기기 자신이 보고
  //   그 화면에는 승인 버튼이 없다. 승인자용 '승인하지 마세요' 를 재사용하면 "여기서 뭘 승인하나" 로
  //   읽혀 지시 대상이 어긋난다 → 누르지 말아야 할 곳(기존 기기)을 명시한다. 보안 지시는 유지.
  //  ★ 개정 5: 대기 화면은 **스피너 + 안내 2줄**이다(코드·새로고침 버튼 삭제). 승인은 WS(resolved)로
  //   즉시 반영되므로 사용자가 누를 것이 없고, `later` 는 5초 뒤에만 나타난다(먼저 보여 주면 승인을
  //   기다리지 않고 빠져나가는 길이 기본값이 된다). 코드는 여기서도 `appr.reveal` 로 꺼낼 수 있다 —
  //   그러지 않으면 승인하는 기기만 코드를 볼 수 있어 **대조 자체가 불가능**해진다.
  wait: {
    title: '내 PC에서 승인해 주세요',
    //  (개정 10: `titleFromMobile` 삭제 — PC 의 `이 기기` 섹션에서 대기 지시문을 없앴으므로 짝 문구가
   //   가리킬 화면이 사라졌다. 대기 안내는 앱 연동 안내 화면(link.*)과 승인 시트가 맡는다.)
    sub: '이미 로그인된 기기에 요청을 보냈어요',
    later: '나중에',
    noSafety: '안전 코드를 아직 못 만들었어요 · 기존 기기에서 승인하지 마세요',
  },

  //  ★ 2026-07-28 개정 8(사용자 확정) — 모바일 첫 로그인의 **연동 안내**(온보딩식 전체 화면).
//   원문: "그래도 그 전에 사용자한테 android에서 내 pc 목록에 승인 요청할까요? 라고 물어보면서 뭔가
//   온보딩 식으로 알려줘야 하지 않을까? … 승인 요청을 보낸 거니까 연동할 pc에 요청을 보냈어요! 확인 후
//   승인해주세요! 라는 식으로".
//   온보딩 슬라이드 문법(2026-07-15 확립)을 따른다: **1주제 · 단일 CTA · 즉시 적용 · 자동 진행**.
//    ① ask  — 물어본다(요청은 아직 아무 기기에도 가지 않았다: services/e2ee.ts announce:false)
//    ② sent — 보냈다 + 무엇을 기다리는지(스피너). 여기서도 `나중에` 로 빠져나갈 수 있다.
//    ③ done — 연동됐다(짧게 보여 주고 자동으로 워크스페이스로 — 사용자가 누를 것 없음)
//   ⚠ 이 화면은 **계정에 PC 가 있을 때만** 뜬다: 계정 첫 기기면 서버가 bootstrap 을 주고 스스로 열쇠를
//    만든다(state='bootstrap' → 'trusted') → 승인해 줄 기기가 없으므로 물어볼 것도 없다.
  link: {
    askTitle: '승인된 내 PC가 없어요',
    askBody: '내 PC에서 승인하면 이 기기와 연동돼요.\n지금 승인 요청을 보낼까요?',
    askCta: '내 PC에 승인 요청 보내기',
    sentTitle: '내 PC에 요청을 보냈어요',
    sentBody: 'PC 화면에 뜬 알림에서 [승인]을 눌러 주세요.',
    doneTitle: '연동됐어요',
    later: '나중에',
    sending: '보내는 중…',
    hint: '나중에 설정 > 계정 > 기기에서도 연동할 수 있어요.',
  },

  err: {
    approve: '승인하지 못했어요',
    link: '연동 요청을 보내지 못했어요',
    deny: '거절하지 못했어요',
    revoke: '해제하지 못했어요',
  },
} as const;

export default E2EE_COPY;
